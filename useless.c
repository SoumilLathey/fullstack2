/*#include <stdio.h>

int main() {
    int arr[6] = {1, 3, 4, 6, 5, 0};  // Ensures 6 elements
    int i;
    int number[6] = {1, 2, 3, 4, 5, 6};
    int temp;

    for (i = 0; i < 6; i++) {
        if (arr[i] != number[i]) {
            temp = arr[i];
            arr[i] = number[i];
            number[i] = temp;
        }
    }

    printf("Modified arr: ");
    for (i = 0; i < 6; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");

    return 0;
}
*/
#include <stdio.h>
int main(){
    int size = 6;
    
}